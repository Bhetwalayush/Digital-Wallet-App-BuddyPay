import 'package:flutter/material.dart';

class RequestCredits extends StatelessWidget {
  const RequestCredits({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Request credits Page"),
    );
  }
}
