import 'package:flutter/material.dart';

class SendCredits extends StatelessWidget {
  const SendCredits({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Send Credits Page"),
    );
  }
}
