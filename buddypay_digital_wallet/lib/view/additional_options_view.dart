import 'package:flutter/material.dart';

class AdditionalOptions extends StatelessWidget {
  const AdditionalOptions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Additional Option Page"),
    );
  }
}
